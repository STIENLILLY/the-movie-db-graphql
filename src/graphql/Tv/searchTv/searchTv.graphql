type searchTvResponse {
    id: Int!
    name: String!
    poster_path: String!
    backdrop_path: String
    overview: String!
    release_date: String!
    popularity: Float!
    vote_count: Int!
    vote_average: Float!
}

type Query {
    searchTv(query: String!): [searchTvResponse]!
}
